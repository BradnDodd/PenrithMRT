version: "2.0"

services:
  penrithmrt-frontend:
    container_name: penrithmrt-frontend
    build:
      context: ./
      target: penrithmrt
    volumes:
      - ./src:/var/www/html
    user: 1000:1000
    ports:
      - 80:80
      - 5173:5173